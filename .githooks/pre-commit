#!/usr/bin/env bash
set -euo pipefail

staged=$(git diff --cached --name-only --diff-filter=ACMR)

if [ -z "$staged" ]; then
    exit 0
fi

# Pass staged files to the shared validation script (single source of truth)
bash scripts/validate-content.sh $staged
